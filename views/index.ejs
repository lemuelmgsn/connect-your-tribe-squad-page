<%- include('./partials/head') %>

<!-- <h2>Students</h2>
<ul>
  <% persons.forEach(person => { %>
    <li><a href="./person/<%= person.id %>"><%= person.name %></a>, squad_id <%= person.squad_id %></li>
  <% }) %>
</ul> -->

<div class="filter">
  <p>Filter op:</p>
  <ul>
    <button>Kapsels</button>
    <button>Avatars</li>
    <button>Woonplaats</button>
    <button>...</button>
  </ul>
</div>

<!-- <article class="bericht-article">
  <h2>Berichten van studenten</h2>
<ul>
  <% messages.forEach(message => { %>
    <li><%= message %></li>
    <% }) %>
</ul>
</article>

<form method="post" action="/">
<label>Berichtje: 
  <textarea required name="bericht"></textarea>
</label>  
<button type="submit">Verzenden</button>
</form> -->

<section class="cards">
  <h2>Avatars</h2>
    <ul>
      <% persons.forEach(person => { %>
        <a href="./person/<%= person.id %>">
        <li>
          <img src="<% if (person.avatar) { %>
            <%= person.avatar %>
          <% } else { %>
            /images/transparent person black.png
          <% } %>">
        </li>
        <%= person.name %>
        </a>
      <% }) %>
    </ul>
</section>

<!-- <div class="flip-card">
  <div class="flip-card-inner">
    <div class="flip-card-front">

      
        <img src="https://www.w3schools.com/howto/img_avatar.png" alt="Avatar" style="width:300px;height:300px;">

    </div>
    <div class="flip-card-back">
      <h2>Berichten van studenten</h2>
      <ul>
        <% messages.forEach(message => { %>
          <li><%= message %></li>
          <% }) %>
      </ul>
      <form method="post" action="/">
        <label>Bericht:
          <textarea required name="bericht"></textarea>
        </label>  
        <button type="submit">Verzenden</button>
        </form>
    </div>
  </div>
</div> -->


   


















<%- include('./partials/foot') %>